{
  "name": "spacebear",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "cd client && npm install && cd ../server && npm install",
    "start": "concurrently 'npm run server' 'npm run client'",
    "server": "cd server && nodemon dist/server.js",
    "client": "cd client && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
